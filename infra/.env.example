# Copier ce fichier en .env et ajuster si besoin (valeurs par défaut déjà fonctionnelles en local).
# .env est exclu du versioning (voir .gitignore) : ne jamais y mettre de vrais secrets de production.

POSTGRES_USER=panafripay
POSTGRES_PASSWORD=panafripay
POSTGRES_DB=panafripay
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
